<h1>Listing tags</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @tags.each do |tag| %>
  <tr>
    <td><%=h tag.name %></td>
    <td><%= link_to 'Show', tag %></td>
  </tr>
<% end %>
</table>

<br />
<%= link_to 'List of posts', posts_path %>